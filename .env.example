# K_Supervisor local environment template
# GPT Store Edition does not require developer-owned secrets.
# Copy this file to .env only for optional standalone/API integrations or local development.

K_SUPERVISOR_ENV=development

# Optional standalone/API provider secrets. Keep values empty in the tracked template.
OPENAI_API_KEY=
SEARCH_API_KEY=
DATABASE_URL=
